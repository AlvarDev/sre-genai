apiVersion: skaffold/v4beta11
kind: Config
metadata:
  name: sre-genai-minikube
build:
  local:
    push: false
  artifacts:
    - image: catalog-mcp-server
      context: catalog-mcp
      docker:
        dockerfile: Dockerfile
manifests:
  rawYaml:
    - k8s/catalog-mcp.yaml
deploy:
  kubectl: {}
portForward:
  - resourceType: Service
    resourceName: catalog-mcp-service
    port: 8001
    localPort: 8001
